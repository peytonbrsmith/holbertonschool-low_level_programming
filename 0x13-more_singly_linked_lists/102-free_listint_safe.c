#include "lists.h"
/**
 * free_listint_safe - frees a linked list
 * @h: double pointer to head
 * Return: the size that was free'd
 */

size_t free_listint_safe(listint_t **h)
{
	h = h;
	return (0);
}
