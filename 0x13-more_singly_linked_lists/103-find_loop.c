#include "lists.h"
/**
 * find_listint_loop - finds a loop in a linked list
 * @head: double pointer to head
 * Return: the address of the node that starts it
 */

listint_t *find_listint_loop(listint_t *head)
{
	head = head;
	return (head);
}
