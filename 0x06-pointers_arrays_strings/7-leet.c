#include "holberton.h"
#include <stdio.h>

/**
 * leet - encodes into 1337
 * @s: passed string
 *
 * Return: char pointer
 */
char *leet(char *s)
{
	while (s[x] != '\0')
	{
		
		x++;
	}
    return (s);
}
